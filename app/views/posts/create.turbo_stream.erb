<%#= turbo_stream.prepend dom_id(current_user, :posts), partial: 'posts/post', locals: { post: @post }%>
<%= turbo_stream.replace dom_id(current_user, :new_post), partial: 'posts/form', locals: { post: Post.new } %>